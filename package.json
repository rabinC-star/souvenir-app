{
  "name": "sovernir-app",
  "version": "1.0.0",
  "description": "Souvenir photo printing app with authentication and email notifications",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"next dev -H 0.0.0.0\"",
    "dev:backend": "cd backend && npm run dev",
    "build": "next build",
    "start": "next start",
    "install:all": "npm install && cd backend && npm install"
  },
  "keywords": ["photo", "printing", "souvenir"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
